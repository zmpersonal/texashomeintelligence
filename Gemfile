source "https://rubygems.org"

# Matches the Jekyll + plugin versions GitHub Pages builds with in production.
gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-sitemap"
  gem "jekyll-seo-tag"
end
